doctype html
html
  head
    title Search User
  body
    include navbar.pug
		
.container
    .row
      .col-xs-7.offset-xs-3
        h1.text-xs-center Search Existing Users
        hr
.container
    .row
      .col-xs-4.offset-xs-3
        form(action='/route3', method='post')
          .form-group
            input(type="text" name='name' placeholder="Name" class="form-control")
            .form-group
            button(type="submit" class="btn btn-success btn-lg btn-") Find
p #{searchresult}
